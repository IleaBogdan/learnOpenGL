# GLAD

https://glad.dav1d.de/

Select gl as 4.6 (or whatever version you need).
Profile must be core.
And hit generate.
Downloadt the zip and extract it in the project.

# GLFW3

```sh
# sudo apt-get -y install libglfw3
# sudo apt -y install libglfw3
sudo apt install libglfw3-dev
sudo apt install libglu1-mesa-dev freeglut3-dev libxi-dev
```
